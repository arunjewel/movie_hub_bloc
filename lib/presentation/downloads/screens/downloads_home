import 'package:flutter/material.dart';

class DownloadsHome extends StatelessWidget {
  const DownloadsHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SafeArea(
          child: Center(
        child: Text("Downloads"),
      )),
    );
  }
}